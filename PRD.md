# PRD: Knowledge Log (지식 정리 웹페이지)

## 1. 개요

프로그래밍 및 자기계발 관련 학습한 지식을 체계적으로 정리하고 관리하는 웹페이지.
마크다운 기반 에디터로 지식을 입력하고, 태그/검색으로 빠르게 찾아보는 것이 핵심.

## 2. 목표 사용자

- 프로그래밍 공부를 꾸준히 하는 개발자
- 자기계발 학습 기록을 관리하고 싶은 사용자
- 대규모(1000개 이상) 지식 항목을 체계적으로 정리할 필요가 있는 사용자

## 3. 핵심 기능

### 3.1 지식 항목 CRUD

| 필드 | 타입 | 설명 |
|------|------|------|
| 제목 | string | 지식 항목 제목 |
| 내용 | string (마크다운) | 상세 내용 |
| 요약 | string | 간략 요약 (자동 생성 또는 수동 입력) |
| 출처 | string | 학습 출처 (URL, 책 제목 등) |
| 난이도 | enum | 초급/중급/고급 |
| 태그 | string[] | 텍스트 태그 목록 |
| 생성일 | date | 자동 생성 |
| 수정일 | date | 자동 갱신 |

기능:
- 항목 추가/수정/삭제
- 목록 뷰 (카드형/리스트형 토글)
- 상세 뷰

### 3.2 마크다운 에디터

- 실시간 미리보기 (split view)
- 문법 하이라이팅 (CodeMirror 또는 Monaco Editor)
- 이미지 첨부 (Base64 인코딩, 로컬 스토리지 저장)
- 코드블록 지원 (언어별 구문 강조)
- 수학 수식 지원 (KaTeX)
- 단축키 지원 (볼드, 이탤릭, 코드블록 등)

### 3.3 태그 시스템

- 단순 텍스트 태그
- 태그 자동 완성 (기존 태그 목록에서 제안)
- 태그별 필터링
- 태그 사용 빈도 표시 (사이드바 또는 태그 클라우드)

### 3.4 검색

- 제목 + 내용 + 태그 통합 검색
- 실시간 검색 (디바운스 적용)
- 검색 결과 하이라이팅
- 정렬: 최신순, 오래된순, 이름순

### 3.5 데이터 관리

- 로컬 스토리지 (localStorage/IndexedDB)에 저장
- JSON 내보내기/가져오기 (백업/복원)
- 대규모 데이터 최적화: 가상 스크롤, 페이지네이션

## 4. 기술 스택

| 구분 | 기술 |
|------|------|
| 프레임워크 | Next.js 14+ (App Router, TypeScript) |
| UI | React 18+, Tailwind CSS |
| 폰트 | Pretendard (CDN) |
| 에디터 | CodeMirror 6 |
| 수학수식 | KaTeX |
| 가상 스크롤 | react-virtuoso |
| 상태관리 | React Context + useReducer |
| 데이터 저장 | localStorage + JSON 백업 |
| 배포 | GitHub Pages (GitHub Actions CI/CD) |

**라이브러리 선정 근거:**
- react-virtuoso: 동적 높이 항목 자동 지원, 그룹핑/스티키 헤더 내장, 무한 스크롤 내장, 주간 다운로드 ~2.1M
- CodeMirror 6: 경량, 확장성 좋은 마크다운 에디터
- KaTeX: 빠른 수학 수식 렌더링

## 5. 디자인

### 5.1 스타일: 글래스모피즘

- 반투명 배경 (`backdrop-filter: blur`)
- 연한 테두리 (`border: 1px solid rgba(255,255,255,0.18)`)
- 배경 그라데이션 또는 이미지
- 그림자 효과

### 5.2 색상 팔레트

- 라이트 모드: 밝은 그라데이션 배경 + 반투명 카드
- 다크 모드: 어두운 그라데이션 배경 + 반투명 카드
- 시스템 설정 자동 감지 (`prefers-color-scheme`)

### 5.3 레이아웃 (데스크톱 중심)

```
┌─────────────────────────────────────────────┐
│  Header (로고 + 검색바 + 다크모드 토글)      │
├──────────┬──────────────────────────────────┤
│  Sidebar │  Main Content                    │
│  - 태그   │  - 목록 뷰 / 상세 뷰            │
│  - 필터   │  - 에디터 모달                   │
│  - 통계   │                                  │
└──────────┴──────────────────────────────────┘
```

### 5.4 주요 화면

1. **메인 목록**: 카드형 지식 항목 표시 (제목, 요약, 태그, 난이도)
2. **상세 뷰**: 마크다운 렌더링 + 메타 정보
3. **에디터 모달**: 마크다운 에디터 + 메타 입력 폼
4. **사이드바**: 태그 목록, 필터, 통계 (전체 항목 수, 카테고리별 분포)

## 6.非기능적 요구사항

| 항목 | 요구사항 |
|------|----------|
| 성능 | 1000개 이상 항목에서도 부드러운 스크롤/검색 |
| 호환성 | Chrome, Firefox, Safari 최신 버전 |
| 접근성 | 키보드 네비게이션, 스크린리더 지원 |
| SEO | 메타 태그, Open Graph (정적 HTML) |
| 배포 | GitHub Pages 자동 배포 (push to main → 빌드 → 배포) |

## 7. 개발 단계

### Phase 1: 기본 뼈대
- Next.js 프로젝트 초기화
- 글래스모피즘 테마 + Pretendard 폰트 적용
- 레이아웃 (헤더, 사이드바, 메인)
- 로컬 스토리지 CRUD

### Phase 2: 에디터 & 검색
- 마크다운 에디터 통합 (CodeMirror)
- 실시간 미리보기
- 태그 시스템
- 범용 검색

### Phase 3: 고도화
- 대규모 데이터 최적화 (가상 스크롤)
- JSON 백업/복원
- 다크모드 자동 감지
- 단축키 지원

### Phase 4: 배포
- GitHub Actions CI/CD 설정
- GitHub Pages 배포
- 성능 최적화

## 8. 확정 사항

- 이미지 첨부: Base64 로컬 저장 확정
- 수학 수식: KaTeX 도입 확정
- 가상 스크롤: react-virtuoso 확정
- 리포지토리: https://github.com/asher8554/web_study
